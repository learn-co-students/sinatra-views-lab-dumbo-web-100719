 <% t = Time.now %>

# <% date = Date.today.strftime("%A, %B %d, %Y") %>

<h1> Today </h1>

 <p> The date is <%= t.strftime("%A") %>, <%= t.strftime("%B") %> <%= t.strftime("%d") %>, <%= t.strftime("%Y") %> </p>

# <p>The date is <%= date %> </p>
